module.exports = (token) => {
  // pegaToken();
  if (token.simbolo === 'sidentificador') {
    // pegaToken();
    if (token.simbolo === 'sponto_virgula') {
      // analisaBloco();
    } else {
      // ERRO
    }
  } else {
    // ERRO
  }
};
