module.exports = (token) => {
  // pegaToken();
  // analisaExpressao();
  if (token.simbolo === 'sentao') {
    // pegaToken();
    // analisaComandoSimples();
    if (token.simbolo === 'ssenao') {
      // pegaToken();
      // analisaComandoSimples();
    }
  } else {
    // ERRO
  }
};
